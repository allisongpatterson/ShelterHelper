<h2>Individual Animal's Page*</h2>

<table class='table table-striped'>
  <thead>
    <th>Id</th>
    <th>Name</th>
    <th>Age</th>
    <th>Sex</th>
    <th>Shelter</th>
  </thead>
  <tbody>
    <tr>
      <td><%= @animal.id %></td>
      <td><%= @animal.name %></td>
      <td><%= @animal.age %></td>
      <td><%= @animal.sex %></td>
      <td><%= @animal.shelter_id %></td>
    </tr>
  </tbody>
</table>

<p>* lack of design is only temporary, forgive me</p>