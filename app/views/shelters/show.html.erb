<h2>Individual Shelter's Page*</h2>

<table class='table table-striped'>
  <thead>
    <th>Id</th>
    <th>Name</th>
    <th>City</th>
    <th>Phone</th>
  </thead>
  <tbody>
    <tr>
      <td><%= @shelter.id %></td>
      <td><%= @shelter.name %></td>
      <td><%= @shelter.city %></td>
      <td><%= @shelter.phone %></td>
    </tr>
  </tbody>
</table>

<table class='table table-striped'>
  <thead>
    <th><%= sort_by :id %></th>
    <th><%= sort_by :name %></th>
    <th><%= sort_by :age %></th>
    <th><%= sort_by :sex %></th>
    <th><%= sort_by :shelter_id %></th>
    <th></th>
  </thead>
  <tbody>
    <%- @shelter_animals.each do |animal| -%>
      <tr>
        <td><%= animal.id %></td>
        <td><%= animal.name %></td>
        <td><%= animal.age %></td>
        <td><%= animal.sex %></td>
        <td><%= animal.shelter_id %></td>
        <td><%= link_to "view details", animal %></td>
      </tr>
    <%- end -%>
  </tbody>
</table>

<p>* lack of design is only temporary, forgive me</p>