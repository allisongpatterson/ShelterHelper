<h2>Shelter Index Page</h2>

<table>
  <thead>
    <th><%= sort_by :id %></th>
    <th><%= sort_by :name %></th>
    <th><%= sort_by :city %></th>
    <th>Phone</th>
  </thead>
  <tbody>
    <%- @shelters.each do |shelter| -%>
      <tr>
        <td><%= shelter.id %></td>
        <td><%= shelter.name %></td>
        <td><%= shelter.city %></td>
        <td><%= shelter.phone %></td>
      </tr>
    <%- end -%>
  </tbody>
</table>